# Canvas Lab 新手运行与使用教程

Canvas Lab（画布实验室）是一个本地优先的开源创作工作台。它把无限画布、图像/视频/音频节点、3D 导演台、资产库和可选的模型服务连接放在同一个浏览器界面里。项目不绑定账号、会员或固定模型，服务地址和密钥由使用者自己配置。

## 1. 启动项目

### Windows

1. 完整解压项目目录，建议使用 `D:\Infinite-Canvas\` 这类短路径。
2. 有便携 Python 时直接双击 `启动服务.bat`。
3. 没有依赖时先运行 `安装依赖.bat`，完成后再次启动服务。
4. 浏览器打开 `http://127.0.0.1:3000/`。命令行窗口需要保持打开。

也可以在 PowerShell 中运行：

```powershell
python -m pip install -r requirements.txt
python main.py
```

### macOS / Linux

在项目目录执行：

```bash
python3 -m pip install -r requirements.txt
python3 main.py
```

然后打开 `http://127.0.0.1:3000/`。如果系统 Python 版本过低，请先安装 Python 3.10 或更高版本。

## 2. 配置第一个模型服务

1. 启动后先看首页。左侧「创作」拆剧本，「画布」进项目，「连接」里打开 API 设置。
2. 点击“新增 API”，填写便于识别的名称。
3. 选择服务协议：OpenAI 兼容、异步协议、Gemini、火山引擎、RunningHub、ModelScope、CLI 或本地 ComfyUI。
4. 填写自己的 Base URL、API Key 和模型 ID。地址可以是云端服务，也可以是本机地址。
5. 点击“验证地址”，确认请求可达后点击“拉取模型”。
6. 在模型选择器中勾选图像、视频和聊天模型，点击保存。

Canvas Lab 不代替使用者创建第三方账号，也不会在界面中附带推广链接。需要令牌或密钥时，请按照所选服务的官方规则获取，并只保存在自己的本机环境中。

### 常用本地地址

```text
Canvas Lab: http://127.0.0.1:3000
ComfyUI:    http://127.0.0.1:8188
```

如果服务运行在局域网的另一台电脑，将 `127.0.0.1` 换成对应内网地址即可。

## 3. 创建并运行画布

1. 在首页点击“新建画布”，输入名称。
2. 点击工具栏的“添加节点”，可添加角色、场景、文本、图片、视频、音频和 3D 导演台。
3. 拖动节点调整布局；从输出端拖到输入端建立连线。
4. 在节点中填写提示词、尺寸、时长和模型，点击参考槽添加本地图片/视频/音频。
5. 视频节点支持首帧、末帧、构图、角色、风格、背景和运镜参考；同一条视频还可以按时间段填写分段提示词。
6. 点击节点运行按钮。请求会通过所选适配器发送，结果以媒体卡片回写到节点。
7. 画布会自动保存；右上角可以导出 JSON，便于备份或分享可复现的工作流。

## 4. 3D 导演台

3D 导演台是画布中的独立节点。打开后：

- 左侧切换对象、角色、道具、机位、动作、运镜和 AI 识图。
- 中间视口支持鼠标拖拽和 WASD/QE 移动，调整角色、道具与镜头关系。
- 右侧显示当前机位预览，底部时间线用于动作和镜头段落。
- 可以保存姿势、机位、运动路径和构图截图，并把截图送回视频节点的布局参考槽。
- 修改完成后点击“应用到画布”，导演台数据会作为结构化元数据进入视频生成适配器。

未知字段会被适配器忽略，因此可以使用不同模型服务而不改变现有请求骨架。

## 5. 资产库与本地文件

- “资产”页面集中查看生成结果、导入素材和画布快照。
- 可以把本地文件拖入资产库，再从节点参考槽选择。
- Chrome 本地素材桥用于把浏览器下载或剪贴板素材导入项目；Photoshop 资产桥用于交换当前文档中的图层或导出图像。
- 资产默认留在本机运行目录。发送到上游服务前，节点会明确显示使用的参考素材。

## 6. 音频和视频工作流

视频工作流支持 ADR（对白）、SFX（音效）和 BGM（音乐）三类音轨。可在时间线中调整起止时间、音量和淡入淡出。

视频节点的工具条提供抽帧、提升画质、裁取、音频分离、裁剪、解析、拉片和抠像等本地操作。需要局部重拍时，选择时间段并框选画面，输入一句修改说明即可生成替换片段。相邻视频卡片可以启用“末帧续接”，自动把上一段末帧作为下一段参考。

## 7. 导入、导出和备份

- 画布：导出/导入 JSON。
- 视频工作流：导出 JSON 或 ZIP，ZIP 会包含工作流描述和引用清单。
- 资产：在资产库中逐项下载，或使用批量下载。
- 配置：复制 `.env.example` 为 `.env`，不要把真实 Key 提交到版本库。

## 8. 故障排查

### 页面打不开

确认 `main.py` 仍在运行，并访问 `http://127.0.0.1:3000/`。端口被占用时先停止旧服务，或者在启动参数中换一个空闲端口。

### 验证地址失败

检查 Base URL 是否包含正确的协议前缀和版本路径，确认 Key 没有多余空格。某些服务不提供 `/v1/models`，此时可以直接填写模型 ID 和自定义生成路径，不以验证按钮结果作为唯一判断。

### 模型列表为空

重新点击“拉取模型”，检查浏览器控制台和命令行日志。ModelScope、异步服务和本地 ComfyUI 的模型列表格式可能不同，请确认协议选择与服务实际接口一致。

### 生成任务没有结果

确认参考文件仍存在、节点输入已连线、ComfyUI 工作流的必需节点已填写。长任务可在“任务日志”中查看提交、轮询和下载阶段。

## 9. 开发者入口

前端页面位于 `static/`，后端入口是 `main.py`。新增模型适配器时，请优先复用现有 provider、工作流 schema 和 adapter，不要在节点中写死特定平台或模型。提交前运行：

```powershell
python -m py_compile main.py
python tests/test_video_workflow.py
python tests/test_xyq_canvas_smoke.py
node tests/video_workflow_unit.js
```

Canvas Lab 的目标是让个人用户在自己的电脑上自由组合模型和工作流，界面、数据和服务连接都由使用者掌控。
